import React, { useEffect, useState, useRef } from "react";
const Score = ({ level, score }) => {
  return (
    <div>
      {level}
      {score}
      {level}
    </div>
  );
};

export default Score;
